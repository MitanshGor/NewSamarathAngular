import { AuthBehaviourServicePipe } from './auth-behaviour-service.pipe';

describe('AuthBehaviourServicePipe', () => {
  it('create an instance', () => {
    const pipe = new AuthBehaviourServicePipe();
    expect(pipe).toBeTruthy();
  });
});
